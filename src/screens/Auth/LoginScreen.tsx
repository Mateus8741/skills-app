import { ImageBackground } from 'react-native';
import BGImg from '~/assets/BGImage.png';

export function LoginScreen() {
  return (
    <ImageBackground source={BGImg} className='flex-1 -m-5 bg-green-600'>
      
    </ImageBackground>
  );
}
